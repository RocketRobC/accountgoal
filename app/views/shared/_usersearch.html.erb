<div class="search">
          <%= form_tag(users_path, :method => "get", id: "search-form") do %>
        <div class="searchbar">
          <%= text_field_tag :search, params[:search], placeholder: "Search Users", class: "form-control"%>
        </div>
          
          <button type="submit" class="btn btn-default"><% submit_tag "Search", :name => nil %>Search</button>
          <% end %>
</div>

<%# form_tag(users_path, :method => "get", id: "search-form") do %>
	<%# text_field_tag :search, params[:search], placeholder: "Search Users" %>
	<%# submit_tag "Search" %>
<%# end %>

<% if params[:search] %>
	<% if @users.present? %>
		<%= render @users %>
	<% else %>
		<p>There are no users with the name <%= params[:search] %></p>
	<% end %>
<% end %>


