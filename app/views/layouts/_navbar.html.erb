<nav class="navbar navbar-default">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="<%= home_index_path %>"><strong>Goalkeeper</strong></a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
            <li><%= link_to " ", new_goal_path, class: "glyphicon glyphicon-plus" %></li>
          <% if current_user.profile %>
            <li><%= link_to "Goal Feed", home_index_path %></li>
            <li><a href="<%= home_index_path(filter: 'incomplete') %>"><span class="badge"><%= current_user.goals.incomplete.count %></span></a></li>
            <li class="dropdown">
              <a class="dropdown-toggle" data-target="#" href="<%= profile_path(current_user.profile) %>" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= cl_image_tag(current_user.profile.profileimage, :width => 30, :height => 30, :crop => :thumb, :gravity => :face, :radius => :max, :default_image => "d_avatar.png") %><span class="caret"></span></a>  
                <ul class="dropdown-menu">
                  <li><%= link_to "My Profile", profile_path(current_user.profile) %></li>
                  <li role="separator" class="divider"></li>
                  <li><%= link_to "Sign Out", destroy_user_session_path, method: :delete %></li>
          <% else %>
                  <li><%= link to "Create Profile", new_profile_path %></li>
          <% end %>

        <% else %>
                  <li><%= link_to "Sign In", new_user_session_path %>
                </ul>
            </li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>

<!-- Scripts -->
<script type="text/javascript">
  $('.dropdown').hover(function(){ 
  $('.dropdown-toggle', this).trigger('click'); 
});
</script>